# Session transcripts written at runtime
.rfa/

# Build artifacts
/rfa
/dist/
*.out
*.test

# Local secrets / env (may contain tokens — never commit)
.env
.env.local
env.openai.sh

# Editor / OS
.DS_Store
.idea/
.vscode/
